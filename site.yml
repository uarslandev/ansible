---
# ==============================================================================
# Master Infrastructure Playbook
# ==============================================================================

# 1. Developer Workstation Setup (runs on every workstation)
- import_playbook: workstation.yml

# 2. Server Node Setup (Placeholder for future Home Server / VPS nodes)
- name: Provision Server Nodes
  hosts: servers
  tags: [server]
  tasks:
    - name: Server placeholder task
      ansible.builtin.debug:
        msg: "Server configuration play will execute here when servers are added to inventory.ini"
